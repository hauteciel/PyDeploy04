# AWS 배포 전략 (FastAPI + React, EC2 / RDS / S3)

## 0. 사전 준비: 코드에 반영한 환경(dev/prod) 대응 변경사항

배포 전략을 세우기 전에, dev/prod 환경을 코드 레벨에서 구분할 수 있도록 아래 항목을 먼저 수정했습니다.

| 파일 | 변경 내용 |
|---|---|
| `backend/main.py` | CORS `allow_origins`를 하드코딩(`localhost:5173`)에서 `.env`의 `ALLOWED_ORIGINS`(콤마 구분)로 읽도록 변경. `ENVIRONMENT`에 따라 `uvicorn` 실행 시 host(`127.0.0.1`→`0.0.0.0`)와 `reload` 여부를 자동 전환 |
| `backend/database.py` | `ENVIRONMENT`에 따라 SQL 로그(`echo`)를 dev에서만 켜도록 변경. `DATABASE_URL`은 원래도 `.env`의 `DB_HOST` 등으로 구성되어 있어 RDS 전환 시 `.env`만 바꾸면 됨 |
| `backend/storage.py` (신규) | 파일 저장소를 추상화. `STORAGE_BACKEND=local`이면 기존처럼 `../upload` 디스크에 저장, `STORAGE_BACKEND=s3`이면 S3 버킷에 저장하고 다운로드/조회 시 presigned URL로 리다이렉트 |
| `backend/controllers.py` | 업로드/다운로드/이미지뷰 엔드포인트가 직접 디스크를 다루던 코드를 `storage.py` 호출로 교체 |
| `backend/requirements.txt` | `boto3` 추가 (S3 연동용) |
| `backend/.env`, `.env.example` | `ENVIRONMENT`, `ALLOWED_ORIGINS`, `STORAGE_BACKEND`, `AWS_REGION`, `AWS_S3_BUCKET` 추가 |
| `frontend/src/191_FileUpload/api.js` | axios `baseURL` 하드코딩(`http://127.0.0.1:8000/...`)을 `import.meta.env.VITE_API_BASE_URL`로 변경 |
| `frontend/.env.development`, `.env.production` (신규) | Vite가 빌드 모드별로 자동 로드하는 환경변수 파일. dev는 로컬 백엔드, prod는 실제 배포 도메인을 지정 (배포 전 실제 값으로 교체 필요) |

이제 `.env`의 값만 바꾸면 같은 코드로 dev/prod를 모두 커버합니다. **`frontend/.env.production`의 `VITE_API_BASE_URL`은 실제 도메인/IP로 반드시 교체하세요.**

---

## 1. 배포 아키텍처 옵션 비교

### 옵션 A. EC2 올인원 (Nginx가 정적 파일 + API를 함께 서빙)
- 프론트 빌드 결과물(`dist/`)과 백엔드를 같은 EC2에 배치, Nginx가 `/`는 정적 파일, `/api`는 FastAPI(uvicorn/gunicorn)로 리버스 프록시
- **장점**: 구성이 가장 단순하고 서버 1대로 끝남. 학습/소규모 프로젝트에 적합
- **단점**: 프론트/백엔드가 같은 인스턴스 리소스를 공유해 트래픽 급증 시 서로 영향을 줌. 정적 파일에 CDN 이점이 없음(속도 느림, 글로벌 서비스 시 불리)

### 옵션 B. 프론트는 S3+CloudFront, 백엔드는 EC2 (★ 추천)
- React 빌드 결과물은 S3 정적 웹 호스팅 + CloudFront(CDN)로 서빙
- FastAPI는 EC2에서 Nginx(리버스 프록시) + Gunicorn/Uvicorn workers로 구동
- DB는 RDS(MySQL), 업로드 파일은 S3
- **장점**: 정적/동적 리소스가 분리되어 각자 독립적으로 확장 가능. S3+CloudFront는 정적 호스팅 비용이 매우 저렴하고 전 세계 어디서든 빠름. EC2는 API 요청만 처리하므로 가볍게 운영 가능. 문제(요구사항)에서 요구한 EC2/RDS/S3를 모두 자연스럽게 사용
- **단점**: 프론트/백엔드가 다른 오리진이 되므로 CORS 설정 필요(이미 코드에 반영함), 배포 파이프라인이 2개(S3 업로드, EC2 배포)로 늘어남

### 옵션 C. 컨테이너 기반 (ECS Fargate + ALB, RDS, S3)
- 백엔드를 Docker 이미지로 만들어 ECS Fargate에서 오토스케일링, ALB로 트래픽 분산
- **장점**: 무중단 배포, 오토스케일링, 인프라 관리 부담 최소화
- **단점**: 이 프로젝트 규모(간단한 파일 업로드 서비스) 대비 설정 난이도와 비용이 과함. 학습 곡선 있음

### 추천: **옵션 B**
현재 요구사항(EC2 + RDS + S3, dev/prod 구분)에 가장 부합하고, 비용 대비 효율이 좋으며, 프론트/백엔드 분리 배포로 향후 확장(오토스케일링 전환 등)도 쉽습니다.

---

## 2. Dev / Prod 환경 분리 전략

| 구분 | Dev | Prod |
|---|---|---|
| 목적 | 기능 검증, 통합 테스트 | 실 사용자 서비스 |
| EC2 | t3.micro 1대 (또는 로컬 개발 PC로 대체 가능) | t3.small 이상, 필요 시 오토스케일링 |
| RDS | db.t3.micro, Single-AZ, dev용 별도 DB(`mydb_dev`) 또는 별도 인스턴스 | db.t3.small 이상, Multi-AZ(가용성), 자동 백업 활성화 |
| S3 | `myapp-dev-uploads` 버킷 | `myapp-prod-uploads` 버킷 (버전 관리·수명주기 정책 적용) |
| 도메인/HTTPS | 선택(사설 IP 또는 sub-domain, 자체서명 인증서 가능) | Route53 + ACM 인증서로 HTTPS 필수 |
| 환경변수 | `backend/.env` (ENVIRONMENT=development) | EC2에서는 `.env` 대신 **SSM Parameter Store/Secrets Manager** 사용 권장 |
| 배포 방식 | 수동 `git pull` + 서버 재시작으로도 충분 | GitHub Actions 등 CI/CD로 자동화 권장 |

같은 코드베이스에서 `ENVIRONMENT`, `DB_*`, `ALLOWED_ORIGINS`, `STORAGE_BACKEND` 값만 바꿔서 두 환경을 운영합니다.

---

## 3. 단계별 배포 절차 (옵션 B 기준)

### 1단계. 네트워크 및 보안 그룹 준비
1. VPC 안에 Public Subnet(EC2용), Private Subnet(RDS용) 구성 (기본 VPC로 시작해도 무방)
2. 보안 그룹 생성
   - `sg-ec2`: 인바운드 80/443(전체 허용), 22(SSH, 본인 IP만)
   - `sg-rds`: 인바운드 3306, **소스를 `sg-ec2`로 제한** (RDS를 외부에 노출하지 않음)

### 2단계. RDS(MySQL) 생성
1. RDS 콘솔에서 MySQL 엔진 선택, dev는 Single-AZ/prod는 Multi-AZ
2. 퍼블릭 액세스 **비활성화**, 서브넷 그룹/보안 그룹(`sg-rds`)에 연결
3. 생성된 엔드포인트를 `backend/.env`의 `DB_HOST`에 반영, `DB_USER/DB_PASSWORD/DB_NAME`도 RDS 생성 시 설정한 값으로 반영
4. (최초 1회) EC2에서 백엔드를 기동하면 `database.Base.metadata.create_all()`이 테이블을 자동 생성

### 3단계. S3 버킷 생성 (업로드 파일 저장용)
1. `myapp-<env>-uploads` 버킷 생성, 퍼블릭 액세스 차단은 기본값 유지(비공개)
2. EC2에 부여할 **IAM 역할(Role)** 생성 → `s3:PutObject`, `s3:GetObject`, `s3:HeadObject` 권한을 해당 버킷에만 허용하는 정책 연결
   - 액세스 키를 코드/환경변수에 직접 넣지 말고 EC2에 IAM 역할을 붙이면 `boto3`가 자동으로 자격 증명을 가져옵니다
3. `backend/.env`에 `STORAGE_BACKEND=s3`, `AWS_S3_BUCKET=myapp-<env>-uploads`, `AWS_REGION` 설정

### 4단계. EC2 백엔드 배포
1. EC2 인스턴스 생성 시 위에서 만든 IAM 역할 연결, `sg-ec2` 적용
2. 인스턴스 접속 후 Python, git, (선택) nginx 설치
3. 저장소 clone → `backend` 디렉터리에서 가상환경 생성 후 `pip install -r requirements.txt`
4. `backend/.env` 작성 (`ENVIRONMENT=production`, RDS 엔드포인트, `ALLOWED_ORIGINS=https://<프론트 도메인>`, `STORAGE_BACKEND=s3` 등)
5. Gunicorn으로 Uvicorn 워커 구동을 systemd 서비스로 등록 (예: `gunicorn main:app -k uvicorn.workers.UvicornWorker -b 127.0.0.1:8000`), 재부팅 시 자동 시작 설정
6. Nginx를 리버스 프록시로 앞단에 두고 `443 → 127.0.0.1:8000` 프록시, Let's Encrypt(certbot)로 HTTPS 적용
7. `curl https://api.example.com/api/files` 등으로 정상 응답 확인

### 5단계. 프론트엔드 빌드 및 S3+CloudFront 배포
1. `frontend/.env.production`의 `VITE_API_BASE_URL`을 4단계에서 만든 실제 API 도메인으로 수정
2. `npm run build` → `frontend/dist` 생성
3. 정적 호스팅용 S3 버킷(`myapp-frontend`) 생성 → `dist` 내용을 업로드 (`aws s3 sync dist/ s3://myapp-frontend`)
4. CloudFront 배포 생성, 오리진을 해당 S3 버킷으로 지정(OAC 사용 권장), 기본 루트 객체 `index.html`, SPA 라우팅을 위해 403/404 → `/index.html`로 리다이렉트 설정
5. ACM에서 도메인 인증서 발급 후 CloudFront에 연결, Route53에서 CloudFront 배포로 CNAME/A(Alias) 레코드 연결

### 6단계. 통합 확인 및 dev 환경 반복
1. 브라우저에서 프론트 도메인 접속 → 업로드/목록조회/다운로드/이미지뷰 전체 플로우 테스트
2. 백엔드 CORS 오류가 나면 `ALLOWED_ORIGINS`에 실제 프론트 도메인이 정확히 들어있는지 확인
3. dev 환경은 위 절차를 축소판(예: EC2 1대에 RDS/S3 dev 리소스 연결)으로 동일하게 구성해 QA 용도로 유지

### 7단계 (선택). CI/CD 자동화
- GitHub Actions로 `main` 브랜치 push 시:
  - 프론트: `npm run build` → S3 sync → CloudFront invalidation
  - 백엔드: EC2에 SSH 접속(또는 CodeDeploy)하여 `git pull` + 서비스 재시작
- 비밀값(RDS 비밀번호, AWS 키 등)은 GitHub Secrets 또는 SSM Parameter Store로 관리하고 리포지토리에는 절대 커밋하지 않습니다 (`.env`는 이미 `.gitignore`에 포함되어 있음)

---

## 4. 비용/운영 참고
- S3+CloudFront 정적 호스팅은 EC2로 프론트를 서빙하는 것보다 훨씬 저렴하고 관리 부담이 없습니다
- RDS는 dev에서 굳이 별도 인스턴스를 두지 않고 prod RDS에 스키마만 분리(`mydb_dev`)해도 되지만, 실습 목적이 아니라면 완전히 분리된 인스턴스를 권장합니다 (dev 작업이 prod 성능에 영향 주지 않도록)
- 업로드 파일 최대 용량 2MB 제한은 그대로 유지되므로 S3 비용 부담은 크지 않습니다. 다만 오래된 파일 정리를 위해 S3 수명주기 정책(예: 1년 후 Glacier 이동) 설정을 고려하세요
